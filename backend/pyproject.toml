[project]
name = "osdc-forum"
version = "0.1.0"
description = "The backend for OSDC Forum."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "sqlmodel>=0.0.39",
]

[dependency-groups]
dev = [
    "uvicorn>=0.52.1",
]
